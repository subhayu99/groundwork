# Every value has a type. Python infers it from how you write the value.

age = 7                 # @sync: int
price = 3.14            # @sync: float
name = "Ada"            # @sync: str
is_adult = age >= 18    # @sync: bool
print(type(price))      # @sync: type
